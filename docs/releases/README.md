# 发布说明维护约定

每次发布新版本前，必须在本目录新增一份中文更新说明：

```text
docs/releases/vX.Y.Z.md
```

发布 workflow 会读取与 tag 同名的文件作为 GitHub Release 正文；如果文件不存在，发布会失败。程序检查更新时也会在 GitHub Release 正文为空或只能走备用更新路径时，按 tag 读取同一份说明。

建议内容包含：

- 本次修复或新增的主要内容。
- 对用户可感知行为的影响。
- 升级建议或需要注意的兼容性变化。
